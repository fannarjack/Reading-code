function greet(name) {
  document.getElementById("message").innerHTML = "Hello, " + name;
}

greet("John");

//here the value of name can be change so it displays Hello Name(the name can be anything i change it to)